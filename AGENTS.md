# AGENTS.md

이 문서는 BridgeWork 프론트엔드 프로젝트에서 에이전트가 작업할 때 가장 먼저 확인해야 하는 기준 문서이다.

자세한 기능, 스타일, 코드 품질 기준은 `.agents/docs` 하위 문서를 따른다.

## 참조 문서

- 프론트엔드 코드 품질 기준: `.agents/docs/frontend-quality-agent.md`
- 기능 명세서 및 활용 데이터: `.agents/docs/specification.md`
- 스타일 가이드: `.agents/docs/style-guide.md`
- 보안 정책: `.agents/docs/security.md`

## 기본 작업 원칙

React 코드를 작성하거나 수정할 때는 단순히 화면이 동작하는 수준이 아니라, 실제 서비스 운영과 접근성을 고려해 구현한다.

특히 다음 항목을 우선한다.

- 렌더링 성능
- API 요청 상태 관리
- loading/error/empty/success UI
- 중복 요청 방지
- 유지보수 가능한 컴포넌트 분리
- 타입 안정성
- 접근성
- React 이전 가능성

## API 호출 원칙

프론트엔드는 Spring Backend만 호출한다.

FastAPI AI/GIS Service는 프론트엔드에서 직접 호출하지 않는다.

기본 호출 흐름은 다음과 같다.

React Frontend → Spring Backend → FastAPI AI/GIS Service

## API 상태 처리 원칙

API 데이터를 사용하는 화면은 다음 상태를 반드시 구분해서 구현한다.

- loading
- error
- empty
- success

데이터가 존재한다고 가정하고 바로 렌더링하지 않는다.

특히 추천 공고 목록, 공고 상세, 접근성 지도, 기업 정보, 사용자 필터 화면은 로딩/에러/빈 데이터 상태를 반드시 제공한다.

추천/지도처럼 백엔드 비동기 계산을 사용하는 화면은 다음 원칙을 추가로 지킨다.

- `POST /recommend/quick`, `POST /recommend/map` 응답이 `PROCESSING`이면 `requestId`로 `/recommend/tasks/{requestId}`를 polling한다.
- `PROCESSING` 상태에 포함된 부분 `result`는 즉시 화면에 반영한다.
- AI 계산 중에는 상단 로딩바에 `불러오는 중 n/100`처럼 현재 배치 진행률을 표시한다.
- 캐시된 추천 결과는 단계 애니메이션 없이 최대 1000개까지 즉시 반영한다.
- AI ON 상태는 무한스크롤 대신 `100개 더 불러오기` 버튼을 사용하고, 최대 1000개까지만 표시한다.
- AI OFF 상태는 서버 페이지네이션과 프론트 무한스크롤을 사용한다.

## 접근성 원칙

BridgeWork는 장애인 구직자를 위한 서비스이므로 WCAG 2.2 AA 수준의 접근성을 고려한다.

다음 원칙을 지킨다.

- 색상만으로 상태를 표현하지 않는다.
- 지도 정보는 목록으로도 제공한다.
- 버튼, 탭, 필터, 카드에는 충분한 클릭 영역을 확보한다.
- 아이콘 버튼에는 aria-label을 제공한다.
- 입력 오류는 구체적인 문장으로 안내한다.
- 데이터가 부족한 항목은 “불가능”으로 단정하지 않고 “확인 필요”로 표현한다.

## UI/UX 원칙

BridgeWork의 UI는 Pretendard 기반의 크고 명확한 인터페이스를 지향한다.

기능 3 지역 접근성 지도 화면을 대표 화면으로 보고, 다음 구조와 톤을 유지한다.

- 좌측 필터 패널
- 중앙 지도 영역
- 우측 상세 패널
- 하단 주요 액션 버튼
- 파란색 중심의 안정적인 공공 서비스 느낌
- 접근성 점수와 이유를 함께 보여주는 카드형 UI

## 컴포넌트 작성 원칙

페이지 컴포넌트에 모든 로직을 몰아넣지 않는다.

권장 분리 기준은 다음과 같다.

- page: 라우팅 단위 화면
- components: 재사용 UI
- api: Spring Backend API 호출 함수
- hooks: API 상태 및 화면 로직
- utils: 공통 유틸 함수
- config: 환경변수와 상수

## 금지 사항

다음 구현은 피한다.

- API 상태 처리 없이 data만 바로 렌더링하는 코드
- 에러를 console.log만 하고 사용자에게 보여주지 않는 코드
- 지도만 제공하고 목록 대체 UI를 제공하지 않는 구조
- 색상만으로 GOOD/CAUTION/RISK를 표현하는 UI
- 장애 유형만으로 업무 가능 여부를 단정하는 문구
- 데이터가 없다는 이유로 접근 불가능하다고 표현하는 문구
- 프론트엔드에서 FastAPI를 직접 호출하는 구조
- 선택 입력이어야 할 민감 정보를 필수로 강제하는 구조
- 한 페이지 파일에 모든 API 로직과 UI를 몰아넣는 구조
- 중복 제출을 막지 않는 폼

## 최우선 기준

BridgeWork 프론트엔드는 단순 구인공고 목록 서비스가 아니라, 장애인 구직자의 실제 지원 가능성과 접근성 판단을 돕는 서비스이다.

따라서 모든 작업에서 다음 세 가지를 최우선으로 둔다.

1. 사용자가 이해할 수 있는 추천 이유 제공
2. 접근성 정보의 안전한 표현
3. 데이터 부족 상황에서도 신뢰할 수 있는 UX 제공
